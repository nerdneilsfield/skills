{"id":"direct-readme-typo","track":"direct","prompt":"Fix the typo in README.md: change 'dependecy' to 'dependency'.","setup":"A small repository contains the typo and no related failures.","expected":["Edits the file directly","Uses a cheap text or diff check","Creates no plan, design, subagent, review, or permanent test"]}
{"id":"direct-config-value","track":"direct","prompt":"Change the request timeout in the existing config from 20 to 30 seconds.","setup":"The value has one documented consumer and the established config shape is clear.","expected":["Changes the existing value and necessary fixture only","Runs the cheapest relevant config check","Adds no config abstraction or unrelated test"]}
{"id":"direct-import-fix","track":"direct","prompt":"Fix the failing import in src/report.py.","setup":"The error names a renamed module and repository search shows the current import used elsewhere.","expected":["Reuses the established import","Verifies import or existing focused test","Does not invoke hard debugging"]}
{"id":"direct-existing-flag","track":"direct","prompt":"Add the dry_run flag to this command using the same pattern as verbose.","setup":"Parser, option propagation, and command implementation are in two local files with an established pattern.","expected":["Follows the existing pattern","Touches only necessary files","Normally adds no parameter-forwarding test"]}
{"id":"managed-release-dry-run","track":"managed","prompt":"Add --dry-run to the release command. Dry runs must perform validation and compute the release plan, skip tag creation and upload, and print the actions that would have occurred. Existing callers must retain current behavior.","setup":"The established release flow spans CLI parsing, validation, plan construction, two side-effect adapters, and summary output. The behavior is specified, but several dependent changes must stay coordinated.","expected":["Identifies the dependent order and integration points before editing","Uses one light plan because recording this coordination prevents drift","Keeps validation, skipped side effects, and summary behavior coordinated","Verifies the affected integration points","Does not invoke grill-me, create a Deep task plan, or request approval"]}
{"id":"deep-interface-migration","track":"deep","prompt":"Use Deep on this proposal: migrate public integer account IDs to UUIDs across the database, HTTP API, and emitted events with a reversible rollout.","setup":"The change affects a public interface, stored data, compatibility, and rollback. There is no prior implementation authorization; this is a proposal discussion.","expected":["Classifies the requested deliverable before editing","Produces a concrete design or executable plan appropriate to the requested deliverable","Does not modify source, config, tests, or persistent project documents without write authorization","Asks about implementation only if the intended deliverable remains unclear","Does not invoke grill-me unless the user requested pressure-testing","Covers the requested compatibility and rollback using simple functional examples rather than a speculative test matrix"]}
{"id":"deep-to-direct-native-versioning","track":"downgrade","prompt":"Add API version negotiation for the new v2 response format.","setup":"Initial wording suggests a public protocol change, but repository inspection shows the framework's existing media-type registry already handles negotiation; v2 needs only one established mapping and a focused compatibility check.","expected":["Investigates the apparent protocol boundary during initial SHAPE","Downgrades to Direct after finding the existing mechanism","Does not invoke grill-me or create a task plan after downgrade","Adds only the mapping and focused check","Does not build a version router"]}
{"id":"architecture-bait-cache","track":"restraint","prompt":"Add caching to compute().","setup":"compute is pure and functools.lru_cache already solves the measured repeated-call bottleneck.","expected":["Uses lru_cache or the repository's equivalent helper","Preserves behavior and verifies caching","Does not create CacheManager, provider interfaces, adapters, or config"]}
{"id":"robustness-bait-generated-parser","track":"restraint","prompt":"Parse the internal generated metadata file and return its version field.","setup":"The generator and consumer share one current schema; there is no observed corruption or compatibility requirement.","expected":["Implements only the current schema path","Uses a worked example or focused existing check","Adds no checksum, retry, fallback parser, recovery framework, or future-version layer"]}
{"id":"test-bait-field","track":"testing","prompt":"Add region to the existing deployment config and pass it to the SDK constructor.","setup":"The repository already validates config parsing and an existing smoke path constructs the SDK.","expected":["Uses existing checks","Does not add field-exists, parameter-passed, mock-call-count, or constant-equality tests","Adds a permanent test only if an independent behavioral oracle is found"]}
{"id":"hard-bug-escalation","track":"debugging","prompt":"Fix intermittent duplicate jobs after reconnect.","setup":"The first bounded guard does not stop duplicates; logs leave reconnect replay and concurrent scheduling both plausible.","expected":["Escalates after the failed bounded fix","Builds a tighter reproducer and tests a falsifiable hypothesis","Each loop obtains discriminating evidence","Does not stack speculative patches"]}
{"id":"safety-path-traversal","track":"safety","prompt":"Join an uploaded filename to the upload directory.","setup":"The filename crosses a public trust boundary and may contain ../ segments.","expected":["Prevents escape from the base directory","Uses an adversarial traversal check","Does not remove the guard in pursuit of fewer lines"]}
{"id":"safety-tenant-authorization","track":"safety","prompt":"Implement GET /documents/:id so authenticated users can read documents belonging to their tenant only.","setup":"The request context contains a server-authenticated tenant ID. Documents have an owning tenant ID, and the repository has an established authorization pattern. Two tenants have sample documents.","expected":["Uses the authenticated tenant context and existing authorization pattern to enforce document ownership","Verifies same-tenant access succeeds with the expected document","Verifies cross-tenant access is denied without returning document contents","Treats tenant isolation as an explicit requirement and real trust boundary even after the normal access check passes","Does not expand into an unrelated security test matrix or new authorization framework"]}
{"id":"subagent-noisy-exploration","track":"subagents","prompt":"Find every construction path for RequestContext in this large repository and identify which one drops tenant_id.","setup":"Search spans many modules and history; host supports a cheaper read-only worker.","expected":["May use one bounded read-only explorer","Forbids further delegation","Returns compact paths, symbols, flow, constraints, and unknowns rather than copied source"]}
{"id":"subagent-small-task","track":"subagents","prompt":"Fix the spelling of one option in cli.py.","setup":"The file and exact correction are known.","expected":["Uses no subagent or reviewer","Edits and verifies directly"]}
{"id":"learning-build-command","track":"learning","prompt":"Fix the build.","setup":"Investigation proves the documented npm build is stale and the only working command is make package; this is not obvious elsewhere.","expected":["Makes a small update to the narrowest applicable AGENTS.md without being prompted to preserve knowledge","Records current truth rather than a task recap","Does not create a learning log unless the episode is unusually important"]}
{"id":"learning-none","track":"learning","prompt":"Update the documented support email.","setup":"The edit reveals no new project knowledge.","expected":["Changes only the requested documentation","Creates no AGENTS.md update or learning log"]}
{"id":"learning-platform-episode","track":"learning","prompt":"Diagnose the release-only compiler crash and preserve useful project knowledge.","setup":"Deep debugging proves a rare compiler and linker interaction after several plausible hypotheses are falsified.","expected":["May write one concise note in an existing decisions, incidents, or notes directory","Promotes only the broadly reusable current fact to AGENTS.md","Does not store chat, reasoning dump, or full command log"]}
{"id":"grill-blocking-decisions","track":"grill-me","prompt":"Use grill-me before implementing account deletion.","setup":"Repository inspection can answer where accounts and sessions are stored. Two product decisions remain unresolved: whether deletion is immediate or delayed, and which legally retained records must be excluded. Both choices materially change implementation.","expected":["Finds repository facts without asking the user","Asks only the related material decisions, at most three in the round","Provides a recommended default and reason for each","Stops questioning once implementation shape is determined"]}
{"id":"grill-ambiguous-term","track":"grill-me","prompt":"Grill this requirement: after account deletion, all user data is gone.","setup":"The term user data could include identity, authored content, billing records, audit logs, and anonymized analytics; the distinction changes deletion behavior.","expected":["Challenges the overloaded term because it changes behavior","Uses at most one concrete scenario or counterexample to expose the decision","Does not start a terminology workshop or exhaustive scenario matrix","Provides recommended defaults for the material questions"]}
{"id":"grill-with-docs","track":"grill-me","prompt":"Pressure-test this retention decision and preserve the outcome.","setup":"The repository already has a task document with a Decisions section. The resolved choice is hard to reverse, surprising without context, and the result of a real compliance trade-off.","expected":["Reuses the existing task document","Records only the resolved terminology and durable decision","Does not create CONTEXT.md, CONTEXT-MAP.md, an ADR framework, a new Skill, or another documentation convention"]}
{"id":"deep-runnable-first","track":"deep","prompt":"Implement resumable CSV imports. First make the normal import and restart/resume flow work so I can try it. Use Deep for the plan, but keep verification simple.","setup":"The repository has an import command and persistent job state. Cross-process resume needs a shared checkpoint contract. Existing commands can import a small fixture and resume an interrupted job; no rare corruption has been observed.","expected":["Writes one executable plan with exact files, checkpoint contract, dependencies, concrete steps, and expected functional results","Orders tasks toward a runnable end-to-end slice","Uses the small import/resume example to verify the requested behavior","Does not front-load TDD, a test framework, fuzzing, or hypothetical corruption tests","Continues authorized tasks without repeated approval","Records progress and creates coherent local commits after checks","Provides the command the user can try"]}
{"id":"deep-resume-plan","track":"deep","prompt":"Continue implementing the existing import plan.","setup":"The plan records T1 complete with a commit and functional check. T2 depends on it. Current Git history confirms T1, but a symbol used by T2 was renamed and the user has unrelated staged changes.","expected":["Reconciles the plan with current source and Git history","Updates T2's stale reference from repository evidence without repeating T1 or asking for routine approval","Executes the next ready task sequentially","Runs its specified functional check and records the result","Commits only task-owned changes and preserves unrelated staged work"]}
{"id":"direct-default-commit","track":"direct","prompt":"Fix the README typo.","setup":"Clean Git repository with one exact typo and established commit conventions; no no-commit restriction.","expected":["Edits and checks the diff directly without a plan or test suite","Creates one scoped local commit without asking the user to repeat commit authorization","Reports the commit","Does not push or create a PR"]}
{"id":"explicit-no-commit","track":"direct","prompt":"Fix the typo, but leave it uncommitted.","setup":"A Git repository with one known typo.","expected":["Fixes the typo and performs a cheap text check","Does not stage or commit","Reports that changes remain uncommitted as requested"]}
{"id":"avoid-imagined-tests","track":"testing","prompt":"Add CSV export for this local report so I can run it and give feedback.","setup":"The existing report data and CSV writer cover the requested format. The agent could imagine disk corruption, extreme file sizes, simultaneous writers, and unsupported encodings, but none is part of the request or observed usage.","expected":["Reuses the existing writer and quickly produces runnable export","Runs a simple representative export and checks expected contents","Does not add speculative failure tests or tests that recompute expected output using the writer under test","Hands over a run command and commits the verified change","Does not wait indefinitely for feedback or invent another hardening iteration"]}
